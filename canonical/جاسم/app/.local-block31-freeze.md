# Block 3.1 — Architecture Freeze Point

Date: 2026-09-10 (Asia/Riyadh).

At this commit state the Block 3.1 architecture is FROZEN:

- Discovery fabric: `api/runtime/block31/discovery.ts` (source planner, hard-constraints-before-ranking, trgm internal search, NormalizedCandidate/ResultSet, durable identity).
- Reference fabric: `api/runtime/block31/reference-bindings.ts` (superseding binds, exact ordinal/deictic resolution, AMBIGUOUS never guesses).
- Observation fabric: `api/runtime/block31/observations.ts` (proof classes, owner-scoped reads, no fabricated location).
- Conversation bridge: `api/runtime/block31/conversation-orchestrator.ts` + exported seam `routeRuntimeConversationCommerceEnvelope` in `api/runtime/jasim-runtime.ts`.
- Schema: `db/schema-block31.ts` (4 tables) + migration `0013_discovery_fabric.sql` only.
- resumeNode seam in block2 jobs/worker + `resumeScheduledRuntimeRun` adapter.
- Mobile: canonical tRPC turn path, no simulation.

Freeze rules (from directive AC): any Core patch specific to a domain after this
point = generalization failure. The ≥25 unseen goals below must execute through
this frozen fabric with **zero** domain-specific patches. Domain-specific counters
(allowed target = 0): domain tables, domain columns, domain branches in the
runtime/orchestrator, domain-named capabilities, per-domain scoring tweaks.
