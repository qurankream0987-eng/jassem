RECOMMENDED REPLIT MODE: FREE

# JASIM SMART GENERATIVE EXPERIENCE
# TASK 9A — ACTIVE GENERATED-UI SECURITY BOUNDARY CLOSURE
# RAW HTML / ARBITRARY EXECUTION / UNTRUSTED CONTENT

MODE INTENT:
Focused security closure for ACTIVE Smart Generative UI paths only.

DO NOT start Final Integration.
DO NOT deploy.
DO NOT redesign JASIM.
DO NOT reopen Blocks 0–3.1.
DO NOT rewrite the renderer/runtime architecture.

Current accepted state:

Task 1 through Task 8B2B = CLOSED / PASS.

Web and Mobile now share:

- PresentationDefinition
- runtime validation
- semantic Presentation Decision
- generic renderers
- Active Workspace semantics
- Morphing semantics
- LivingObjectProjection
- Trusted Action Dispatcher
- canonical server truth

Task 8B2B readiness reconciliation:

READINESS_RECONCILIATION = PASS
MANDATORY_GATES_PASS = 18/18
EXACT_BLOCKER = NONE
SMART_UI_TASK_8B2B = PASS
READY_FOR_SMART_UI_TASK_9 = YES
PRODUCTION_CODE_CHANGED = NO

Evidence debt carried forward only:

MOBILE_REAL_VISUAL_SMOKE = PARTIAL
VISUAL_CROSS_DEVICE_SMOKE = NOT_AVAILABLE

Do NOT treat those as Task 9A implementation blockers.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. GOAL
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Close the remaining ACTIVE security paths by which generated/provider/external content could bypass the validated semantic presentation system.

Required final architecture:

UNTRUSTED CONTENT
↓
NORMALIZATION / DATA EXTRACTION
↓
VALIDATED PRESENTATION CONTRACT
↓
ALLOWLISTED COMPONENT RESOLUTION
↓
SAFE RENDERER
↓
TRUSTED ACTION DISPATCHER

NOT:

LLM HTML
→ browser

NOT:

provider HTML
→ dangerouslySetInnerHTML

NOT:

component name from model
→ dynamic import

NOT:

generated JS
→ eval/new Function

NOT:

generated action
→ arbitrary endpoint/provider URL

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2. SCOPE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Audit ONLY active Smart Generative Experience paths.

Web:

PresentationRenderer
SchemaRenderer
generic presentation primitives
ActiveGenerativeWorkspace
Living Objects / Active Objects Rail
Smart Bubble active paths
generated document/media presentation
generated external/search result presentation
generated World presentation

Mobile:

MobilePresentationRenderer
MobileWorkspaceSurface
Mobile Living Objects surface
generated document/media surfaces
generated action rendering

Shared/client-safe contracts involved in those paths.

Do NOT perform a repository-wide generic XSS cleanup unrelated to active JASIM Smart UI.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3. PRIMARY INVARIANT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

MODEL / PROVIDER / EXTERNAL CONTENT IS DATA.

It is never trusted UI code.

An LLM/provider may supply:

text
structured fields
URLs validated by policy
media references
semantic presentation data
typed action intents

It may NOT supply executable:

HTML
JS
React
component implementation
event handler
server handler path
SQL
CSS injection
provider execution URL

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4. RAW HTML AUDIT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Search active Smart UI paths for:

dangerouslySetInnerHTML

innerHTML

outerHTML

insertAdjacentHTML

HTML string rendering

markdown allowing raw HTML

HTML returned by providers

HTML generated by model

HTML stored in presentation metadata

iframe srcdoc

webview HTML

Report every ACTIVE occurrence.

Classify:

SAFE_STATIC_INTERNAL
SANITIZED_UNTRUSTED_CONTENT
UNSAFE_ACTIVE_PATH
LEGACY_OUTSIDE_SMART_UI

Do not count test strings as runtime paths.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
5. REQUIRED RAW HTML OUTCOME
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Target:

UNSAFE_ACTIVE_RAW_HTML_PATHS = 0

If active Smart UI currently requires formatted text:

prefer structured content / safe markdown subset / escaped text.

Do NOT introduce a new sanitizer package unless current dependencies already provide one and it is clearly necessary.

If a current sanitizer exists:

reuse it.

Do not build a weak regex sanitizer.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
6. MARKDOWN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

If Markdown is supported:

ensure model/provider Markdown cannot inject raw executable HTML.

Allowed formatting may include:

headings
lists
links
code
tables
emphasis

according to current product needs.

Raw HTML must remain escaped/disabled unless passed through an already-trusted robust sanitizer.

Links remain data.

Do not allow:

javascript:
data:text/html
arbitrary scriptable URI schemes

where actionable.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
7. ARBITRARY JAVASCRIPT AUDIT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Search active Smart UI paths for:

eval(

new Function(

Function constructor

dynamic script injection

script element creation from generated content

model-generated JS execution

provider-generated JS execution

runtime code string execution

Target:

ARBITRARY_GENERATED_JS_EXECUTION_PATHS = 0

Do not change unrelated build tooling uses unless they intersect active Smart UI execution.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
8. DYNAMIC COMPONENT AUDIT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Verify PresentationDefinition cannot supply arbitrary React/Native component identifiers that are resolved dynamically.

Allowed:

semantic type
→ static allowlisted registry
→ trusted component

Forbidden:

model componentName
→ import(componentName)

provider componentPath
→ require(...)

remote module loading

Target:

UNTRUSTED_DYNAMIC_COMPONENT_PATHS = 0

Web and Mobile both required.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
9. PRESENTATION SCHEMA
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Reconfirm active Web and Mobile renderers only consume runtime-validated PresentationDefinition.

No legacy path should bypass Task 1 validation with:

any
unknown cast
raw provider response
raw assistant metadata
unvalidated JSON

where privileged presentation/actions are rendered.

Target:

ACTIVE_UNVALIDATED_PRESENTATION_RENDER_PATHS = 0

Do not broaden into unrelated legacy pages.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
10. ACTION SECURITY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Task 7 already established Trusted Action Dispatcher.

Task 9A verifies generated UI cannot create an alternate execution path through:

URL actions
deep-link handler names
dynamic mutation names
browser events
native events
HTML onclick
custom script callbacks

Canonical/consequential generated actions must still use:

runtime.dispatchAction

Target:

ACTIVE_GENERATED_ACTION_BYPASSES = 0

Do not rewrite Dispatcher.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
11. URL / LINK SAFETY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Audit generic generated:

links
media
documents
external resources

Validate schemes through existing trusted utility/policy.

Do not allow generated:

javascript:
vbscript:
data:text/html

or equivalent executable schemes.

External HTTP(S) links may remain allowed according to existing product policy.

Do not automatically fetch arbitrary URL from privileged server context.

SSRF remains governed by existing runtime/provider boundaries.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
12. IMAGE / MEDIA
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Media references are data.

Do not allow image/media metadata to inject:

HTML
JS
event handlers
provider secrets
arbitrary native component code

Use trusted native/Web media components.

If remote URL is allowed:

respect existing URL/media policy.

Do not create new proxy architecture.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
13. DOCUMENT PRESENTATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

DOCUMENT presentation may show:

safe extracted text
metadata
download/open actions
trusted preview where supported

Do not render arbitrary uploaded/external HTML as application UI.

If document content is HTML:

treat as document data, not trusted JASIM component markup.

If current active path renders raw document HTML:

close/isolate it.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
14. WORLD GENERATED CONTENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

A generated World may contain:

configuration
entities
offerings
text
presentation metadata

It must NOT contain executable UI code trusted by renderer.

World → authorized projection → PresentationDefinition → renderer.

No:

World HTML
World JS
World React source
World arbitrary component

as privileged execution.

Target:

GENERATED_WORLD_EXECUTABLE_UI_PATHS = 0

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
15. SMART BUBBLES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Inspect Smart Bubble rendering.

Persistent Bubble content must follow same trust rule.

Bubble may carry:

semantic presentation
trusted references
validated actions
safe display data

It must not become an escape hatch for:

raw HTML
custom JS
arbitrary component
direct mutation
provider URL execution

Target:

SMART_BUBBLE_SECURITY_BYPASSES = 0

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
16. WEBVIEW
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Task 8 already established:

WEBVIEW_SMART_UI_PATHS = 0

Reconfirm.

Do NOT introduce WebView as solution for generated content.

Mobile generated UI must remain native.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
17. CROSS-PLATFORM CONTRACT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

The same malicious/invalid PresentationDefinition fixture should fail safely on both:

Web
Mobile

Examples:

unknown component
raw HTML field
script URI
privileged unknown action
handler path
provider URL override

Device-specific renderer must not weaken shared contract security.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
18. ESCAPING / DISPLAY DATA
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

User/model/provider strings displayed in:

title
subtitle
description
attributes
badges
status
timeline
comparison
forms
Living Objects

must render as text/data.

Do not interpolate into HTML templates.

Do not derive executable style/class names from arbitrary strings where that creates injection risk.

Normal React/React Native text escaping is preferred.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
19. CSS / STYLE INJECTION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Generated PresentationDefinition should not accept arbitrary:

style string
CSS
className
Tailwind expression
position/fixed overlay values
z-index attack values

unless explicitly validated through existing limited design tokens.

Prefer semantic design variants:

tone
size
emphasis
layout

mapped to trusted local tokens.

Target:

ARBITRARY_GENERATED_STYLE_EXECUTION = 0

Do not rebuild styling system.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
20. PRIVILEGED PRESENTATION FIELDS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Reject/strip attempts for generated content to specify:

ownerId as authority
policy override
verification result
payment state
trusted provider receipt
server route
SQL
handler
secret
authorization decision

Task 7 protections must remain intact.

Presentation metadata is never authority.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
21. FAIL-CLOSED
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Malformed/unsafe generated presentation:

must not crash entire Conversation.

Preferred result:

safe text fallback
safe ErrorState
blocked action
ignored unsafe field

according to existing contract.

But do NOT silently execute unknown content.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
22. NO NEW SECURITY FRAMEWORK
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Do NOT add:

new sandbox runtime
new plugin framework
new CSP framework rewrite
new HTML sanitizer package
new authorization layer

unless a directly blocking active path cannot be closed with current architecture.

If new dependency appears necessary:

STOP and report it.

Expected:

NEW_EXTERNAL_SDKS = 0

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
23. ACTIVE VS LEGACY
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Important scope distinction.

Report:

ACTIVE_SMART_UI_UNSAFE_PATHS

separately from:

LEGACY_NON_SMART_UI_UNSAFE_PATHS

Task 9A PASS requires:

ACTIVE_SMART_UI_UNSAFE_PATHS = 0

Legacy unrelated paths may be deferred if they are not reachable from the Smart Generative Experience.

Do not hide them.

Do not expand Task 9A into whole-app modernization.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
24. SECURITY TEST FIXTURES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Use focused malicious fixtures such as:

<script>alert(1)</script>

<img src=x onerror=...>

javascript:alert(1)

unknown_component

componentPath

handlerPath

providerUrl

policyOverride

paid=true

verified=true

rawHTML

generatedJS

These are TEST INPUTS only.

Do not place them into production/demo data.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
25. REQUIRED TESTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Add focused tests proving at minimum:

1. raw model HTML renders as escaped/safe data or is rejected.

2. provider HTML cannot become executable UI.

3. markdown cannot execute raw script HTML.

4. javascript: generated link blocked.

5. data:text/html executable link blocked.

6. unknown presentation component blocked.

7. dynamic component path cannot be supplied.

8. model cannot provide React module path.

9. eval/new Function absent from active generated UI path.

10. malformed PresentationDefinition fails closed.

11. unvalidated assistant metadata cannot render privileged presentation.

12. arbitrary generated style/class payload cannot execute privileged styling.

13. unknown action cannot bypass Dispatcher.

14. handler path cannot be supplied.

15. provider URL cannot become action execution target.

16. paid=true cannot establish payment truth.

17. verified=true cannot establish verification truth.

18. policyOverride cannot establish authorization.

19. Smart Bubble cannot render executable HTML.

20. World cannot inject executable UI.

21. Document presentation does not trust raw HTML.

22. Web malicious fixture fails safely.

23. Mobile same malicious fixture fails safely.

24. safe ordinary text still renders.

25. safe Markdown still renders if supported.

26. generic results/comparison/form/approval remain functional.

27. Workspace regression.

28. Living Objects regression.

29. Trusted Action Dispatcher regression.

30. Mobile renderer regression.

Keep the suite focused.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
26. STATIC AUDIT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Inspect active Smart UI code for:

dangerouslySetInnerHTML
innerHTML
outerHTML
insertAdjacentHTML
eval
new Function
dynamic import from untrusted value
require from untrusted value
iframe srcdoc
WebView HTML
javascript:
data:text/html

Do not mechanically fail on every string occurrence.

Prove reachability/context.

Report exact active occurrences.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
27. VISUAL / RUNTIME SMOKE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Verify normal safe UI still loads after hardening:

Web:
Conversation
Workspace
generic generated presentation

Mobile:
Conversation
Mobile Workspace
generic generated presentation

No white screen.
No renderer crash.
No raw JSON fallback in normal case.

No need to exercise every business scenario.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
28. PREVIOUS VISUAL EVIDENCE DEBT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Do NOT attempt to solve these during Task 9A:

MOBILE_REAL_VISUAL_SMOKE = PARTIAL
VISUAL_CROSS_DEVICE_SMOKE = NOT_AVAILABLE

Carry them unchanged to final Power integration.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
29. PREVIOUS CANONICAL ERRORS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Prior work documented unrelated canonical errors.

Do NOT fix them unless one directly blocks Task 9A.

Carry them to final integration.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
30. EXPECTED CHANGES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Expected:

small renderer hardening
safe URL/content helper reuse or extraction
raw HTML isolation/removal
shared schema tightening if required
focused tests

No DB.

No migrations.

No SDK.

No architecture rewrite.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
31. VERIFICATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Run:

Task 9A focused security tests

affected Smart UI regressions

Trusted Dispatcher regression

Web renderer regression

Mobile renderer regression

Web TypeScript

Mobile TypeScript

Server TypeScript if shared/server code changed

Web production build

Metro bundle

git diff --check

Web startup smoke

Mobile startup smoke

Do NOT deploy.

Do NOT run unrestricted parallel PostgreSQL suites if known test DB collision remains.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
32. REPORT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Create exactly:

.local/tasks/smart-ui-task-9a-security-report.md

Sections:

CAUSE

ACTIVE_SMART_UI_SURFACES_AUDITED

RAW_HTML_AUDIT

MARKDOWN_AUDIT

ARBITRARY_JS_AUDIT

DYNAMIC_COMPONENT_AUDIT

PRESENTATION_VALIDATION_AUDIT

ACTION_BYPASS_AUDIT

URL_SCHEME_AUDIT

MEDIA_AUDIT

DOCUMENT_AUDIT

WORLD_AUDIT

SMART_BUBBLE_AUDIT

STYLE_INJECTION_AUDIT

WEB_MOBILE_PARITY

ACTIVE_VS_LEGACY_FINDINGS

FILES_CHANGED

TESTS

RUNTIME_SMOKE

Final counters:

SMART_UI_TASK_9A =
PASS / PARTIAL / FAIL

ACTIVE_SMART_UI_UNSAFE_PATHS =
<number>

UNSAFE_ACTIVE_RAW_HTML_PATHS =
<number>

ARBITRARY_GENERATED_JS_EXECUTION_PATHS =
<number>

UNTRUSTED_DYNAMIC_COMPONENT_PATHS =
<number>

ACTIVE_UNVALIDATED_PRESENTATION_RENDER_PATHS =
<number>

ACTIVE_GENERATED_ACTION_BYPASSES =
<number>

SMART_BUBBLE_SECURITY_BYPASSES =
<number>

GENERATED_WORLD_EXECUTABLE_UI_PATHS =
<number>

WEBVIEW_SMART_UI_PATHS =
<number>

ARBITRARY_GENERATED_STYLE_EXECUTION =
<number>

EXECUTABLE_URL_SCHEMES_BLOCKED =
YES / NO

RAW_PROVIDER_HTML_TRUSTED =
NO / YES

RAW_MODEL_HTML_TRUSTED =
NO / YES

CLIENT_CAN_SET_PAID =
NO / YES

CLIENT_CAN_SET_VERIFIED =
NO / YES

CLIENT_CAN_OVERRIDE_POLICY =
NO / YES

LEGACY_NON_SMART_UI_UNSAFE_PATHS =
<number>

NEW_DATABASE_TABLES =
0

NEW_MIGRATIONS =
0

NEW_EXTERNAL_SDKS =
0

WEB_TYPESCRIPT =
PASS / FAIL

MOBILE_TYPESCRIPT =
PASS / FAIL

SERVER_TYPESCRIPT =
PASS / FAIL / NOT_TOUCHED

WEB_PRODUCTION_BUILD =
PASS / FAIL

METRO_BUNDLE =
PASS / FAIL

WEB_SMOKE =
PASS / FAIL

MOBILE_SMOKE =
PASS / FAIL / PARTIAL

BLOCKS_0_TO_3_1_REMAIN_CLOSED =
YES / NO

READY_FOR_SMART_UI_TASK_9B =
YES / NO

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
33. PASS GATE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Task 9A may be PASS only if:

ACTIVE_SMART_UI_UNSAFE_PATHS = 0

UNSAFE_ACTIVE_RAW_HTML_PATHS = 0

ARBITRARY_GENERATED_JS_EXECUTION_PATHS = 0

UNTRUSTED_DYNAMIC_COMPONENT_PATHS = 0

ACTIVE_UNVALIDATED_PRESENTATION_RENDER_PATHS = 0

ACTIVE_GENERATED_ACTION_BYPASSES = 0

SMART_BUBBLE_SECURITY_BYPASSES = 0

GENERATED_WORLD_EXECUTABLE_UI_PATHS = 0

WEBVIEW_SMART_UI_PATHS = 0

ARBITRARY_GENERATED_STYLE_EXECUTION = 0

EXECUTABLE_URL_SCHEMES_BLOCKED = YES

RAW_PROVIDER_HTML_TRUSTED = NO

RAW_MODEL_HTML_TRUSTED = NO

CLIENT_CAN_SET_PAID = NO

CLIENT_CAN_SET_VERIFIED = NO

CLIENT_CAN_OVERRIDE_POLICY = NO

and affected tests/builds pass.

Legacy non-Smart-UI paths may remain only if explicitly proven outside current Smart Generative Experience reachability.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
34. STOP
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

After Task 9A:

STOP.

DO NOT start Task 9B.

DO NOT remove demo/mock paths yet unless directly required for security closure.

DO NOT start Power integration.

DO NOT deploy.

Return only:

Task 9A verdict

active unsafe path count

raw HTML path count

arbitrary JS path count

dynamic component path count

action bypass count

legacy non-Smart-UI unsafe path count

focused test count

build/typecheck status

READY_FOR_SMART_UI_TASK_9B

BEGIN TASK 9A.